{
  "installCommand": "true",
  "buildCommand": "mkdir -p dist && cp -R chat dist/",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/chat",
      "destination": "/chat/index.html"
    }
  ]
}
