--- a/rust/crates/zkapi-client/src/wallet.rs
+++ b/rust/crates/zkapi-client/src/wallet.rs
@@ -47,7 +47,6 @@
     state: Option<NoteState>,
     state_path: PathBuf,
     journal_path: PathBuf,
-    http: reqwest::Client,
 }
 
 impl Wallet {
@@ -77,7 +76,6 @@
             state,
             state_path,
             journal_path,
-            http: reqwest::Client::new(),
         })
     }
 
@@ -290,8 +288,7 @@
         };
 
         let url = format!("{}/v1/requests", self.config.server_url);
-        let resp = self
-            .http
+        let resp = reqwest::Client::new()
             .post(&url)
             .json(&api_request)
             .send()
@@ -764,8 +761,7 @@
             "{}/v1/requests/{}",
             self.config.server_url, journal.client_request_id
         );
-        let resp = self
-            .http
+        let resp = reqwest::Client::new()
             .get(&url)
             .send()
             .await
@@ -864,8 +860,7 @@
             withdrawal_nullifier: *withdrawal_nullifier,
         };
 
-        let resp = self
-            .http
+        let resp = reqwest::Client::new()
             .post(&url)
             .json(&req)
             .send()
--- a/rust/crates/zkapi-client/src/wallet_v2.rs
+++ b/rust/crates/zkapi-client/src/wallet_v2.rs
@@ -27,7 +27,6 @@
     state: Option<NoteState>,
     state_path: PathBuf,
     journal_path: PathBuf,
-    http: reqwest::Client,
 }
 
 impl Wallet {
@@ -50,7 +49,6 @@
             state,
             state_path,
             journal_path,
-            http: reqwest::Client::new(),
         })
     }
 
@@ -122,8 +120,7 @@
         merkle_siblings: Vec<Felt252>,
     ) -> Result<RequestResponseV2, ClientError> {
         let request = self.prepare_request(payload, payload_hash, active_root, merkle_siblings)?;
-        let response = self
-            .http
+        let response = reqwest::Client::new()
             .post(format!(
                 "{}/v2/requests",
                 self.config.server_url.trim_end_matches('/')
@@ -293,8 +290,7 @@
         let Some(journal) = PendingRequestJournal::read(&self.journal_path)? else {
             return Ok(None);
         };
-        let response = self
-            .http
+        let response = reqwest::Client::new()
             .get(format!(
                 "{}/v2/requests/{}",
                 self.config.server_url.trim_end_matches('/'),
@@ -431,8 +427,7 @@
         &self,
         nullifier: &Felt252,
     ) -> Result<ClearanceResponseV2, ClientError> {
-        let response = self
-            .http
+        let response = reqwest::Client::new()
             .post(format!(
                 "{}/v2/withdraw/clearance",
                 self.config.server_url.trim_end_matches('/')
